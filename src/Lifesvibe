const LandingPage = () => `
  <!doctype html>
  <html lang="en">
    <head>
      <meta charset="utf-8"/>
      <meta name="viewport" content="width=device-width, initial-scale=1"/>
      <title>Lifesvibeplatform</title>
      <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body {
          font-family: 'Segoe UI', system-ui, sans-serif;
          background: #0d0d0d;
          color: #e8e8e8;
          line-height: 1.6;
        }
        nav {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 1.2rem 2.5rem;
          border-bottom: 1px solid #1e1e1e;
          position: sticky;
          top: 0;
          background: #0d0d0d;
          z-index: 100;
        }
        nav .logo {
          font-size: 1.3rem;
          font-weight: 700;
          letter-spacing: 0.05em;
          color: #c9a96e;
          text-decoration: none;
        }
        nav ul {
          list-style: none;
          display: flex;
          gap: 2rem;
        }
        nav ul a {
          color: #bbb;
          text-decoration: none;
          font-size: 0.95rem;
          transition: color 0.2s;
        }
        nav ul a:hover { color: #c9a96e; }
        nav .cta {
          background: #c9a96e;
          color: #0d0d0d;
          padding: 0.5rem 1.2rem;
          border-radius: 4px;
          text-decoration: none;
          font-weight: 600;
          font-size: 0.9rem;
          transition: background 0.2s;
        }
        nav .cta:hover { background: #b8934f; }

        /* Hero */
        .hero {
          text-align: center;
          padding: 7rem 2rem 5rem;
          max-width: 800px;
          margin: 0 auto;
        }
        .hero .tagline {
          font-size: 0.85rem;
          letter-spacing: 0.2em;
          text-transform: uppercase;
          color: #c9a96e;
          margin-bottom: 1.2rem;
        }
        .hero h1 {
          font-size: clamp(2.2rem, 5vw, 3.8rem);
          font-weight: 800;
          line-height: 1.15;
          margin-bottom: 1.5rem;
          color: #fff;
        }
        .hero p {
          font-size: 1.1rem;
          color: #999;
          max-width: 560px;
          margin: 0 auto 2.5rem;
        }
        .hero-actions { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
        .btn-primary {
          background: #c9a96e;
          color: #0d0d0d;
          padding: 0.85rem 2rem;
          border-radius: 4px;
          text-decoration: none;
          font-weight: 700;
          transition: background 0.2s;
        }
        .btn-primary:hover { background: #b8934f; }
        .btn-secondary {
          border: 1px solid #444;
          color: #ccc;
          padding: 0.85rem 2rem;
          border-radius: 4px;
          text-decoration: none;
          transition: border-color 0.2s, color 0.2s;
        }
        .btn-secondary:hover { border-color: #c9a96e; color: #c9a96e; }

        /* Sections */
        section { padding: 5rem 2rem; }
        .container { max-width: 1000px; margin: 0 auto; }
        .section-label {
          font-size: 0.8rem;
          letter-spacing: 0.2em;
          text-transform: uppercase;
          color: #c9a96e;
          margin-bottom: 0.75rem;
        }
        h2 {
          font-size: clamp(1.6rem, 3vw, 2.4rem);
          font-weight: 700;
          color: #fff;
          margin-bottom: 1rem;
        }
        .lead { font-size: 1.05rem; color: #888; max-width: 620px; }

        /* About */
        #about { border-top: 1px solid #1a1a1a; }

        /* Features */
        #features { background: #111; border-top: 1px solid #1a1a1a; }
        .features-grid {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
          gap: 1.5rem;
          margin-top: 3rem;
        }
        .feature-card {
          background: #1a1a1a;
          border: 1px solid #252525;
          border-radius: 8px;
          padding: 1.8rem;
        }
        .feature-card .icon { font-size: 1.8rem; margin-bottom: 1rem; }
        .feature-card h3 { font-size: 1rem; font-weight: 700; color: #fff; margin-bottom: 0.5rem; }
        .feature-card p { font-size: 0.9rem; color: #777; }

        /* Community */
        #community {
          border-top: 1px solid #1a1a1a;
          text-align: center;
        }
        #community .container { max-width: 620px; }
        #community .lead { margin: 0 auto 2rem; }

        /* Footer */
        footer {
          border-top: 1px solid #1a1a1a;
          padding: 2.5rem 2rem;
          text-align: center;
          color: #555;
          font-size: 0.85rem;
        }
        footer a { color: #777; text-decoration: none; margin: 0 0.75rem; }
        footer a:hover { color: #c9a96e; }
        footer .footer-links { margin-bottom: 1rem; }
      </style>
    </head>
    <body>

      <nav>
        <a href="/" class="logo">Lifesvibeplatform</a>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#features">Features</a></li>
          <li><a href="#community">Community</a></li>
        </ul>
        <a href="#contact" class="cta">Sign Up</a>
      </nav>

      <section class="hero">
        <p class="tagline">Wellness &amp; Lifestyle</p>
        <h1>Live the Vibe.<br/>Elevate Your Life.</h1>
        <p>Lifesvibeplatform is your all-in-one space for wellness, mindfulness, and lifestyle inspiration — designed to help you thrive every day.</p>
        <div class="hero-actions">
          <a href="#contact" class="btn-primary">Get Started</a>
          <a href="#about" class="btn-secondary">Learn More</a>
        </div>
      </section>

      <section id="about">
        <div class="container">
          <p class="section-label">About</p>
          <h2>What is Lifesvibeplatform?</h2>
          <p class="lead">We're a wellness and lifestyle platform built for people who want more — more balance, more growth, more connection. Whether you're here for daily mindfulness, fitness guidance, or a community that gets it, you're in the right place.</p>
        </div>
      </section>

      <section id="features">
        <div class="container">
          <p class="section-label">Features</p>
          <h2>Everything you need to thrive</h2>
          <div class="features-grid">
            <div class="feature-card">
              <div class="icon">🧘</div>
              <h3>Mindfulness Tools</h3>
              <p>Guided sessions and daily practices to keep you grounded and focused.</p>
            </div>
            <div class="feature-card">
              <div class="icon">💪</div>
              <h3>Wellness Content</h3>
              <p>Curated lifestyle content tailored to your goals and interests.</p>
            </div>
            <div class="feature-card">
              <div class="icon">🌱</div>
              <h3>Personal Growth</h3>
              <p>Resources and inspiration to help you become the best version of yourself.</p>
            </div>
            <div class="feature-card">
              <div class="icon">🤝</div>
              <h3>Community</h3>
              <p>Connect with like-minded people who share your values and vision.</p>
            </div>
          </div>
        </div>
      </section>

      <section id="community">
        <div class="container">
          <p class="section-label">Community</p>
          <h2>You don't have to do it alone</h2>
          <p class="lead">Join a growing community of people committed to living better. Share your journey, find support, and grow together.</p>
          <a href="#contact" class="btn-primary">Join the Community</a>
        </div>
      </section>

      <section id="contact" style="border-top: 1px solid #1a1a1a;">
        <div class="container" style="text-align:center; max-width: 500px;">
          <p class="section-label">Get Started</p>
          <h2>Ready to elevate your life?</h2>
          <p class="lead" style="margin: 0 auto 2rem;">Sign up to be the first to know when we launch and get early access.</p>
          <form style="display:flex; gap: 0.75rem; flex-wrap: wrap; justify-content: center;">
            <input type="email" placeholder="Enter your email" style="flex:1; min-width:220px; padding:0.8rem 1rem; background:#1a1a1a; border:1px solid #333; border-radius:4px; color:#fff; font-size:0.95rem;"/>
            <button type="submit" style="background:#c9a96e; color:#0d0d0d; border:none; padding:0.8rem 1.5rem; border-radius:4px; font-weight:700; cursor:pointer;">Notify Me</button>
          </form>
        </div>
      </section>

      <footer>
        <div class="footer-links">
          <a href="#about">About</a>
          <a href="#features">Features</a>
          <a href="#community">Community</a>
          <a href="#contact">Sign Up</a>
        </div>
        <p>&copy; ${new Date().getFullYear()} Lifesvibeplatform. All rights reserved.</p>
      </footer>

    </body>
  </html>
`
